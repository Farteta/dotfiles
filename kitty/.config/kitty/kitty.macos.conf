# macOS-specific Kitty overrides.
# Clipboard copy feedback on select is handled by:
# ~/.local/bin/kitty-clipboard-feedback-macos

map cmd+plus   change_font_size all +1.0
map cmd+minus  change_font_size all -1.0
